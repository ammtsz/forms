{
  "name": "forms",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "container",
    "creation",
    "submission",
    "view"
  ],
  "scripts": {
    "start": "pnpm --parallel --filter \"./**\" start",
    "build": "pnpm --parallel --filter \"./**\" build",
    "serve": "pnpm --parallel --filter \"./**\" preview",
    "build:container": "pnpm --parallel --filter \"./packages/container\" build",
    "serve:container": "pnpm --parallel --filter \"./packages/container\" serve",
    "dev:container": "pnpm --filter \"./packages/container\" dev",
    "stop": "kill-port --port 3000,3001,3002,3003"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
